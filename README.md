# GraphQL API – Assignment 8

This project implements a GraphQL API using Apollo Server. The API includes CRUD operations, search functionality, and JWT-based authentication.

## Installation
npm install

## Start the server:
npm start

## Queries
getAllData
getDataById(id)
searchData(query)

## Mutations
createData(forename, surname)
updateData(id, forename, surname)
deleteData(id)
login(username, password)

## Testing
## Run automated tests with:
npm test